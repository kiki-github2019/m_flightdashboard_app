## 향후 작업 시트 (260605_0630)

### 직전 시트 대비 변동 요약
- §1 디자인 누락(F-01~F-07) **모두 구현 완료** — c7e41a5 / 0b5feb4 / b68d86b / 7757cb2.
- §4 Q-03 dead code 정리 완료(3b126ef).
- 파일 라인수 6,500 → **9,216** 으로 증가 → **Q-01 우선순위 상향**.
- 신규 보강: `replacePlotYColumnInPlace`(c7e41a5), `normalizePlotSpec*`/`compactPlotTabsSpec`(7757cb2), `applyLightPanelTitleContrast`(7757cb2), `IsProgrammaticXLim` onCleanup 가드(7757cb2), `applyVideoLoadedUI` 단계별 try/catch 분리(b91de40).

### 범례
- **P0** 차단/필수 · **P1** 중요 · **P2** 권장 · **P3** 미래 검토
- **상태**: 미착수 / 부분 / 검증 대기 / 회귀 위험 / **완료**

---

### 1. 디자인 §3 누락 항목 (구현 상태)

| ID | 항목 | 상태 | 검증 필요 사항 |
|---|---|---|---|
| F-01 | Plot Manager 속성 패널 | **완료** (e5e5b92 + 7757cb2) | X auto/manual 분기, Y 라벨 편집, height 60~600 클램프, applyPlotAxisConfig 의 IsProgrammaticXLim 가드 회귀 |
| F-02 | Plot 단위 삭제/복제 | **완료** (c7e41a5 + 7757cb2) | duplicate 후 cfg.Order 재정렬 정합성 |
| F-03 | Sync "현재 화면값 가져오기" | **완료** (0b5feb4) | F-03 + AVI 미로드 상태 캡처 시 안전성 |
| F-04 | Sync offset preview | **완료** (0b5feb4) | refreshSyncOffsetLabel ValueChangedFcn 누락 케이스 |
| F-05 | Export 복사 대상 파일 목록 미리보기 | **완료** (b68d86b) | EDExpFileTable 새로고침 트리거 + missing 라벨 동기화 |
| F-06 | Project "종료 전 저장 확인" 체크박스 | **완료** (b68d86b) | ProjectConfirmOnClose=false 일 때 dirty + 미적용 timer 강제 적용 경로 |
| F-07 | Project "마지막 저장 시간" 라벨 | **완료** (b68d86b) | ProjectLastSaveText 가 saveProjectFile 성공시에만 업데이트되는지 |
| F-08 | Options `Visible` 컬럼 정식 구현 | 보류(P3) | 디자인 §3.4 명시. 필요 시 displayMeta.isVisible + setupDataUI 필터 |

---

### 2. 테스트 / 검증 (사용자 실행 필요)

| ID | 항목 | 우선순위 | 상태 | 비고 |
|---|---|---|---|---|
| T-01 | `auto_test_runner('Start',1,'End',5)` 실행 → testHook 라우팅 | **P0** | 검증 대기 | 직전 chatgpt 235a6b2 "strict GUI auto-test state validation" 후 효과 확인 |
| T-02 | 50 케이스 점진 실행 → OOM 회귀 | **P0** | 검증 대기 | LoadAvi='lazy' vs 'never' |
| T-03 | 비정상#1 (B09/C09) X축 데이터 전체 범위 확인 | **P0** | 검증 대기 | 45d2d6f + c7e41a5 (in-place 교체) 회귀 |
| T-04 | 비정상#2 (B05/C05) 비디오 패널 visible | **P0** | 검증 대기 | step05 PNG 비교 |
| **T-11** | **YColumn 변경 시 plot/tree 보존 (in-place replace)** | **P0** | **검증 대기** | c7e41a5 회귀. 다양한 YColumn 후보 + 다중 plot 탭에서 |
| **T-12** | **Plot Manager Apply 후 height/YLabel/XAuto/YAuto 영속** | **P1** | 검증 대기 | 7757cb2 normalize/compact 회귀 |
| **T-13** | **Export 탭 파일 목록 + missing 라벨 실시간 갱신** | **P1** | 검증 대기 | b68d86b |
| **T-14** | **Project 탭 자동 저장/종료 확인 체크박스 실제 동작** | **P1** | 검증 대기 | b68d86b — ProjectAutosaveEnabled 토글 시 timer 시작/중지 |
| **T-15** | **applyVideoLoadedUI 단계별 catch 효과** | P1 | 검증 대기 | b91de40 — 5개 step 중 1개 실패 시 나머지 진행 여부 |
| T-05 | 보드 off + 마커 드래그 정성 검증 | P1 | 미실행 | |
| T-06 | dirty-close 저장 취소/실패 시나리오 | P1 | 미실행 | F-06 도입 후 ProjectConfirmOnClose=false 케이스도 추가 |
| T-07 | Export SHA256 토글 실제 검증 | P1 | 미실행 | |
| T-08 | Project 자동 로드 12-step 메시지 | P1 | 미실행 | |
| T-09 | testHook 멤버 변수 일관성 | P2 | 미실행 | |
| T-10 | MATLAB unittest framework 회귀 자동화 | P3 | 미착수 | |

---

### 3. 잠재 위험 / 회귀 가능성

| ID | 위험 | 우선순위 | 상태 | 작업 내용 |
|---|---|---|---|---|
| **R-08** | `findMainPlotLine` 휴리스틱 (Marker='none' + 최대 XData) → xline / 추가 보조선이 우연히 더 길면 오선택 | **P1** | **신규** | line 객체에 tag 부여 (예: `Tag='dataLine'`) 후 findobj |
| **R-09** | `compactPlotTabsSpec` 가 trailing 빈 tab 만 제거 → 중간 빈 tab 인덱스 어긋남 가능 | **P1** | **신규** | tab 인덱스/순서 재정렬 또는 sparse 유지 명시 |
| **R-10** | `applyLightPanelTitleContrast` 는 생성 시점 1회만 호출 → BoardOff 토글 등으로 동적 추가되는 panel 의 title 색상 미보정 | **P2** | **신규** | createBoardOffSummaryPanel 등 동적 생성 경로 끝에 host 별로 호출 또는 panel 생성 헬퍼화 |
| **R-11** | `applyPlotAxisConfig` 의 `cleanupFlag = onCleanup(...)` + 즉시 `clear cleanupFlag` 패턴 — 의도된 명시적 호출이나 onCleanup 의 일반 패턴과 다름 (clear 가 즉시 cleanup 호출) | P2 | 신규 | 명시적 try/finally 또는 onCleanup 가 함수 종료 시 자동 호출되도록 변수 그대로 두는 패턴으로 통일 |
| **R-12** | `replacePlotYColumnInPlace` 가 `plotData{tabIdx}{plotIdx}` 만 갱신, `findMainPlotLine.UserData` 등 다른 캐시 미동기화 | P2 | 신규 | UserData/dataLine 메타가 다른 곳에 있으면 함께 갱신 또는 단일 진실원으로 정리 |
| R-01 | LastDisplayedFrame vs LastDecodedFrame 분리 후 일반 드래그 회귀 | **P1** | 회귀 위험 (미검증) | E01 + 수동 |
| R-02 | restoreBoardPanelState snapshot 무시 → UX 차이 | P1 | 동작 변경 | UX 가이드 |
| R-03 | prefetchFrameToCacheOnly VideoReader 공유 race | P2 | 검토 | |
| R-04 | capturePlotConfigFromUi identity 매칭 by (flight,tab,order) | P2 | 명시된 한계 | drag-reorder UI 도입 시 매칭 키 재설계 |
| R-05 | preserveSync 시 새 AVI TotalFrames 가 다를 때 anchor 범위 밖 | P2 | 미처리 | clamp + 경고 |
| R-06 | `testHook` public 노출 | **P1** | 임시 (테스트 완료 후) | 빌드 플래그 / subclass 이동 |
| R-07 | 50 케이스 PNG quota | P2 | 모니터 | exportapp 해상도 축소 옵션 |

---

### 4. 코드 품질 / 리팩토링

| ID | 항목 | 우선순위 | 상태 | 작업 내용 |
|---|---|---|---|---|
| **Q-01** | **FlightDataDashboard.m 9,216 라인 → 파일/패키지 분할 (이전 6,500 → 41% 증가)** | **P1** 상향 | 미착수 | 우선 분리 후보: ① 모든 `editDialog*` + `buildEditTab*` → +flightdash/editor ② board-off 관련 (toggleBoardVisibility / reflowBoardColumns / off-summary 생성·rebuild) → +flightdash/boardoff ③ video frame 파이프라인 → +flightdash/video |
| **Q-06** | `app.logCaught(ME, 'tag')` 누락-세미콜론 131건 잔존 (75245ae 가 일부만 정리) | P2 | 부분 | catch 블록 들의 silent 호출 후 세미콜론 추가. 동시에 'silent' 태그 분류(`silent.draw`, `silent.fileio`, `silent.cleanup`) |
| **Q-07** | `EDExpFileTable` 의 표시 데이터 새로고침 트리거가 모호 — Export 직전 buildExportFileList 호출 외 외부에서 path 변경 시 미갱신 | P2 | 신규 | requestFileChange / loadAviFileFromPath 성공 후 refreshExportTab 호출 추가 |
| **Q-08** | `normalizePlotSpec` / `normalizePlotSpecArray` / `compactPlotTabsSpec` / `emptyPlotSpec` 네 함수 — 단위 테스트 부재. struct array 필드 정렬 일관성 검증 필요 | P2 | 신규 | MATLAB unittest 로 spec round-trip 검증 |
| Q-02 | silent catch 분류 (UI/로직/cleanup) | P2 | 부분 | UI 호출은 uialert, cleanup 은 silent 유지 |
| Q-04 | `ensureBoardCorePanelsVisible` no-op placeholder | P3 | 주석만 | 향후 PanelVisible.info/H 도입 시 활성 |
| Q-05 | EditDialog UI 핸들 32개 properties → grouped struct (`EDProject.*`, `EDPlot.*` 등) 로 정리 | P3 | 미정리 | namespace 명료화. Q-01 분할과 동시 진행 권장 |

---

### 5. 디자인 §10 결정 항목 후속

| ID | 항목 | 우선순위 | 상태 |
|---|---|---|---|
| D-01 | AVI 자기 자신 덮어쓰기 차단 dialog 메시지 명확화 | P3 | 동작 OK |
| D-02 | autosave snapshot 충돌 시 사용자 복구 UI | P2 | 미착수 |
| D-03 | LinkXWithinTab 시각화 (아이콘/색) | P3 | 텍스트만 |
| D-04 | 대용량 데이터(1M+ 행) hybrid 메모리 측정 | P2 | 미측정 |
| D-05 | "Reset to default mapping" 버튼 | P2 | 미착수 |
| D-06 | Export verification report .md 저장 | P2 | 미착수 |
| D-07 | v2 schema migration | P3 | 자리만 |

---

### 6. 신규 기능 (디자인 외)

| ID | 항목 | 우선순위 | 비고 |
|---|---|---|---|
| N-01 | 단축키 (Ctrl+S 저장, Ctrl+E export) | P3 | uifigure KeyPressFcn |
| N-02 | 한↔영 i18n | P3 | |
| N-03 | 다중 모니터 위치 기억 | P3 | UiState.WindowPosition |
| N-04 | Project diff 도구 | P3 | |
| N-05 | 사용자 매뉴얼 / README | **P1 상향** | 9,216 라인 + 신규 기능 다수 → 사용자 가이드 작성 우선 |
| **N-06** | **EditDialog 의 Plot Manager / Sync / Export 탭 단축 안내 (각 탭 첫 진입 시 1회 tooltip 또는 안내 패널)** | P2 | 신규. F-01~F-07 도입 후 사용자 학습 비용 증가 |

---

### 7. 정적 분석 결과 (현 HEAD = 7757cb2)

| 항목 | 결과 |
|---|---|
| 파일 라인 수 | 9,216 |
| end/function 균형 | end=1,428, function=262 (정상) |
| `%#ok<AGROW>` 잔존 | 16건 (의도된 보존 — 모두 동적 cell/struct 누적 패턴) |
| `logCaught(ME, ...)` 세미콜론 누락 | 131건 (MATLAB lint warning 후보, 무해하나 정리 권장 → Q-06) |
| `find(app.BoardOffState, 1)` 사용처 | 2건 (정상) |
| testHook 노출 | 1건 (R-06 대상) |
| onCleanup 패턴 일관성 | 3종 혼재 (자동 종료 / clear 강제 종료 / finally 풀이) → R-11 통일 |
| `restoreProgrammaticXLim` 헬퍼 | 정상 구현 + 3곳 사용 |
| `applyLightPanelTitleContrast` | createLayout / openEditDialog / createVideoControlDialog 3곳에서 호출. 동적 panel 생성은 미커버 (R-10) |

---

## 즉시 착수 권장 순서

1. **T-01, T-02, T-11** — auto_test_runner + YColumn in-place replace 회귀 (P0)
2. **T-03, T-04** — 비정상 #1/#2 PNG 비교 (P0)
3. **T-12, T-13, T-14** — 신규 도입 F-05/F-06/F-07 동작 검증 (P1)
4. **R-08** — findMainPlotLine 에 Tag 부여 (간단하지만 회귀 차단 효과 큼)
5. **R-09** — compactPlotTabsSpec 중간 빈 tab 처리 정책 명시
6. **R-10** — applyLightPanelTitleContrast 동적 panel 호출 추가
7. **R-11** — onCleanup 패턴 통일 (applyPlotAxisConfig)
8. **R-06** — testHook 프로덕션 제거 (테스트 통과 후)
9. **Q-01** — 9,216 라인 파일 분할 (우선순위 상향, 후속 유지보수 비용 절감)
10. **Q-06** — 세미콜론 누락 일괄 정리 (1회성 sed 가능)
11. **N-05** — 사용자 매뉴얼 작성

## 차단/대기 항목
- 모든 P0 검증은 사용자의 MATLAB 실행 결과 회신 후 진행 가능
- R-06 testHook 제거는 T-01~T-15 모두 통과 후 진행
