# 3D Path 강화 2-커밋 검토 (read-only)

기준 HEAD `7c01c25`. 코드 변경 없음. 대상: `b224496`, `7c01c25`. **참고**: 둘 사이에 `8843258`(*harden 3D path delete cleanup*, FDD +68/-8)이 끼어 있음 — 검토 대상 아님이라 깊이 보지 않았으나, 누적 라이프사이클(B) 판단에 반영.

## 커밋별 요지
- **`b224496`** *harden 3D path scale and regression checks* — (1) 드론 glyph를 **축별 scale→uniform glyphScale**로 변경(#2), (2) full/past trajectory의 point count·XYZ 길이일치·finite 여부를 boardState/`getPath3DState`에 **노출**하고 ATR 검증 추가(이전에 deferred했던 #6 잔여), (3) `getPath3DStateForTest.desiredVisible`를 1x2 정규화. + doc 3종.
- **`7c01c25`** *extend 3D path regression cases* — K-PATH3D **07~10 추가**(F2 렌더, F2 board-off 복원, FlightPlay 동기, **디스크 .fdproj save/load 라운드트립**) + 매크로 `P3DS`/`P3DR` + dispatch `saveLoadPath3DProjectRoundTrip`. 블록 끝에 append → #1 순서 보존.

## 영역별 발견 (A~G)

**A. 의미 정확성** — ✅ 이상 없음
- waypoint(`scatter3` red) / trajectory(`plot3` full 점선 + past 실선) / attitude(RGB tripod) 의미 분리 유지. `getPath3DSeries`는 여전히 `mappedCols.Lon/Lat/Alt`(= body 위치)를 읽음 → **body 위치 의미 정정과 충돌 없음**. #2는 시각 스케일만 변경, 좌표 의미 불변.

**B. 라이프사이클·가드** — ✅ 양호
- `path3DLineState`는 `isempty/isvalid` 가드 + try/catch. `IsDeleting`·partial cleanup 경로 직접 변경 없음(그 강화는 비대상 `8843258`). `delete(app)` 영향 없음.

**C. 옵션·모델·`.fdproj`** — ✅ additive
- `desiredVisible` 1x2 정규화 = `applyLayoutUiState` 정규화(#3)와 **parity**(방어적 일치). 신규 디스크 라운드트립 테스트는 `saveProjectFile`/`autoLoadProjectFromFile` hook 경유 — `i_validateProjectRestore`와 별개 경로라 fixture 비교 비간섭. temp .fdproj는 `i_tempProjectFileRegistry`로 cleanup 등록(L93-94).

**D. 시간 변경·성능** — ✅ 무영향
- `path3DLineState`는 boardState 수집(테스트 경로)에서만 호출, hot path 아님. drone scale은 transform 행렬 1회 계산 — 비용 무시. throttle/decimation/`drawnow` 불변.

**E. UI·메인 GUI** — ✅ 무변경
- 컨트롤바·다이얼로그·반응형 컬럼·축 라벨(East/North/Up) 변경 없음.

**F. 테스트·testHook** — ✅ 정합
- K-PATH3D **10개 전부 매트릭스 끝**(K-PANEL-CAPTURE 뒤) → 후속 카테고리 case 번호 영향 없음(#1 유지). 신규 hook(`saveProjectFile`/`autoLoadProjectFromFile`)·매크로(`FPT/FPM/FPR`)·helper 전부 **존재 확인**. boardState↔`getPath3DState` 노출 일관(full/past × count/XYZconsistent/hasFinite).

**G. 잔존 위험** — 대부분 클린, 1건 주의
- try/catch 균형(ATR 95/95, FDD 693/695 = 기존 주석 내 "catch" 단어 artifact, 신규 불균형 아님). 신규 라인 **한글 0**. 스코프 외 변경 없음(전부 3D-path / doc).

## 우선순위 표

| # | 영역 | 발견 | Severity | 구분 | 권고 |
|---|---|---|---|---|---|
| 1 | A/E | **#2 uniform scale: tripod Z(Up) arm 시각 퇴화** — `path3DAxes`는 auto-stretch + z=Alt(meter)인데 `glyphScale=xySpan*0.035`(degree 크기). 패치 본체(2D 다이아, Z=0)·X/Y arm은 정상이나 **파란 Z arm이 meter 축에서 거의 0 길이**로 보임. attitude shear(Phase 2)는 올바로 해소했으나 현재(attitude-off)에도 tripod는 그려지므로 활성 회귀. | **Medium** | 부분(트레이드오프) | **다음 스프린트**: 좌표를 공통 visual unit으로 정규화하거나 `DataAspectRatio` 데이터기반 고정 후 uniform scale 적용 |
| 2 | F/G | 강화된 `i_validateState`/`assertPath3DRenderState`가 `pastHasFinite` 요구 → render가 past를 항상 ≥1 finite로 채운다는 가정에 의존(`currentIndex` ≥1 clamp로 현재 안전) | **Low** | 새 기능 | **모니터링**: currentIndex 0/비정상 경로 생기면 false-fail 가능성만 관찰 |
| 3 | F | K-PATH3D-10 디스크 I/O(tempname .fdproj) 의존 → 케이스 약간 느림(cleanup은 등록됨) | **Low** | 새 기능 | **모니터링**: 무영향, 회귀 시점만 |
| 4 | C/F | #6 full/past XYZ consistency 노출 + 검증 | — | **이미 반영(강화)** | 즉시 가능: 정상, 조치 불요 |

## 구분 요약
- **이미 반영/강화**: #6 full·past XYZ consistency 노출+검증(deferred한 잔여를 완성), `desiredVisible` 1x2 정규화(#3와 parity), K-PATH3D 순서(#1) 보존.
- **새 기능**: K-PATH3D-07~10(F2/board-off/FlightPlay/디스크 라운드트립), `path3DLineState`, dispatch/매크로.
- **부분(트레이드오프)**: #2 uniform scale — shear 해소 ↔ tripod Z arm 시각 퇴화(표 #1).
- **누락**: 없음.

## 종합
두 커밋 모두 **의미 정확·additive·backward-compatible**, 테스트 커버리지 실질 강화. 유일한 실질 이슈는 **#2의 시각 트레이드오프**(Medium, 다음 스프린트) — attitude 기능을 켜기 전(Phase 2) 좌표 정규화/aspect 고정을 함께 하면 shear 해소와 tripod 가시성을 모두 만족. 나머지는 모니터링 수준.
