

1. D:\flightdashboard\6. python\ 최신 빌드 실행파일 exe를 실행한다.

2. D:\flightdashboard\6. python\sample code\output
위 폴더에 있는 샘플 데이터를 로드하고 GUI 버튼을 동작해서 exe 파일을 테스트한다.

3. 테스트 시나리오(자동생성)에 따라 결과는 화면캡처로 저장한다.

4. 화면 캡처한 결과는 파일명에 순서를 알수 있는 번호가 있어야 한다. 

5. 별도 md 파일로 화면캡처한 파일은 어떤 테스트를 했는지 기술한다.

6. md 파일과 화면 캡처 결과를 리뷰해서 md 파일에 결과를 기록한다. 

7. claude code에 테스트결과를 입력하고 코드를 수정한다. 

위와 같은 절차(1~5) claude cowork에서 작업할수 있는가? 
단 6과 7은 사용자가 직접 claude code에서 할 작업이다.

